= form_for @photo, url: "/photos/#{@photo.id}" do |f|
	= render "form", f: f
	= f.submit "EDIT"



= link_to "DELETE",  {:action => "destroy", :id => "#{@photo.id}"}, :method => :delete, :class => "icon-delete", :confirm => "Are you sure"


